import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  NoticiasScreen: {
    flex: 1,
  },
  image: {
    width: 300,
    height: 200,
    resizeMode: 'cover',
    borderRadius: 10,
    marginBottom: -20,
    marginTop: 20,
  }
});

export default styles;